"""OpenAI provider placeholder for a future milestone."""


class OpenAIProvider:
    """v0.1 stub that prevents accidental real API execution."""

    def __init__(self, *args, **kwargs):
        # Provider stubs keep extension points visible without adding external
        # side effects, credentials, or model-specific behavior to v0.1.
        raise NotImplementedError("OpenAIProvider is a v0.1 stub; use FakeProvider for now.")
