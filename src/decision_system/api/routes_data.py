"""Data API router placeholder.

v0.8 exposes the requested local workflow endpoints without adding new data
mutation routes beyond ontology and insight operations.
"""

from __future__ import annotations

from fastapi import APIRouter


router = APIRouter(tags=["data"])
